# Core Framework
fastapi==0.104.1
uvicorn==0.24.0
python-multipart==0.0.6

# Database
sqlalchemy==2.0.50
psycopg2-binary==2.9.9
alembic==1.13.0

# Supabase (REST API client - used for auth and direct queries)
# Note: supabase-py has Python 3.14 compatibility issues; use httpx for raw API calls
httpx==0.25.1

# Authentication
pydantic==2.5.0
pydantic-settings==2.1.0
email-validator==2.1.0  # required by pydantic EmailStr (routers/organizations.py)
python-jose==3.3.0
passlib==1.7.4
bcrypt==3.2.2
PyJWT==2.8.0

# API & External Services
requests==2.31.0
youtube-transcript-api==1.2.4
yt-dlp>=2024.3.10          # Audio extraction for Whisper transcript fallback
openai>=1.30.0             # Whisper transcription API

# NEW-PACKET-E: document extraction (text-based PDF, Word, URL) + OCR.
# OCR system binaries (tesseract-ocr, poppler-utils) are installed via
# nixpacks.toml; the Python libs below drive them. Extraction still degrades
# gracefully if the binaries are missing in some environment.
pypdf==4.2.0
python-docx==1.1.2
beautifulsoup4==4.12.3
pytesseract==0.3.10
pdf2image==1.17.0
Pillow==10.3.0

# Anthropic & Google APIs
anthropic==0.28.0
google-generativeai==0.3.0

# Environment & Config
python-dotenv==1.0.0

# Scheduling (Packet 3.5 — nightly self-expansion job)
apscheduler==3.10.4

# ADMIN-1.6: 2FA (TOTP) + QR code generation
pyotp==2.9.0
qrcode[pil]==7.4.2

# Testing
pytest==7.4.3
pytest-asyncio==0.23.2
pytest-cov==4.1.0

# Development
black==23.12.0
flake8==6.1.0
mypy==1.7.1
isort==5.13.2

# Logging & Monitoring
python-json-logger==2.0.7
# Error monitoring (Stage 2). Optional at runtime — only active when SENTRY_DSN is set.
sentry-sdk[fastapi]==2.20.0

# CORS is handled by fastapi's built-in CORSMiddleware (via starlette)
