# CI-only requirements for Stage 0
# Only packages needed for linting and running placeholder tests
# Heavy packages (anthropic, google, psycopg2) are excluded until Stage 1

fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0
python-dotenv==1.0.0
sqlalchemy==2.0.23
httpx==0.25.1
pytest==7.4.3
flake8==6.1.0
